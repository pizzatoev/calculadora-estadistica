# Calculadora estadística (frontend)

Frontend React + Vite del monorepo. Las instrucciones completas para levantar **backend + frontend** están en el README de la raíz del proyecto:

**[../README.md](../README.md)**

## Solo este paquete (desarrollo)

```bash
npm install
npm run dev
```

Abre [http://localhost:5173/](http://localhost:5173/) — requiere que la API FastAPI esté corriendo en el puerto **8000** (ver README raíz).
